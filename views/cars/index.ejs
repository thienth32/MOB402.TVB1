<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách ô tô</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
</head>
<body>
    <div class="container-fluid">
        <header class="container">
            <div class="row">
                <div class="col-2">
                    <a href="#">
                        <img src="https://fpt.edu.vn/Content/images/assets/Poly.png" 
                            class="img-fluid">
                    </a>
                </div>
                <div class="col-10">
                    <nav class="navbar navbar-expand-lg navbar-light bg-light ">
                        <div class="collapse navbar-collapse" id="navbarNav">
                          <ul class="navbar-nav">
                            <li class="nav-item ">
                                <a class="nav-link" href="#">Trang chủ <span class="sr-only">(current)</span></a>
                            </li>
                            <li class="nav-item ">
                              <a class="nav-link" href="#">Hãng xe <span class="sr-only">(current)</span></a>
                            </li>
                            <li class="nav-item">
                              <a class="nav-link" href="#">Dòng xe</a>
                            </li>
                          </ul>
                        </div>
                      </nav>
                </div>
            </div>
        </header>
        <main class="container">
            <h4>Danh sách xe ô tô</h4>
            <div class="row">
                <div class="col-4">
                    <form action="" method="get">
                        <div class="form-group">
                            <label for="">Tìm kiếm</label>
                            <input type="text" name="keyword" 
                                value="<%= keyword %>"
                                class="form-control">
                        </div>
                    </form>
                    <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-sm btn-primary">Tìm kiếm</button>
                    </div>
                </div>
            </div>
            <table class="table table-stripped">
                <thead>
                    <th>Tên ô tô</th>
                    <th>Tên hãng</th>
                    <th width="100">Hình ảnh</th>
                    <th width="100">Giá xe</th>
                    <th>
                        <a href="/cars/create" class="btn btn-sm btn-success">Tạo mới</a>
                    </th>
                </thead>
                <tbody>
                    <% for(let i = 0; i < cars.length; i++){ %>
                        <tr>
                            <td><%= cars[i].name %></td>
                            <td><%= cars[i].brand_id.name %></td>
                            <td>
                                <img src="<%= cars[i].image %>" class="img-thumbnail">
                            </td>
                            <td>
                                <%= cars[i].price %>
                            </td>
                            <td>
                                <a href="/cars/update/<%= cars[i].id %>"
                                    class="btn btn-sm btn-primary">Cập nhật</a>
                                &nbsp;
                                <a href="/cars/remove/<%= cars[i].id %>"
                                    onclick="return confirm('Bạn có chắc chắn thực hiện xóa danh mục?')"
                                    class="btn btn-danger btn-sm">Xóa</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </main>
    </div>
</body>
</html>