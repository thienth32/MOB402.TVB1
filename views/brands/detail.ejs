<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách danh mục</title>
    <% include ../_share/style %>
</head>
<body>
    <div class="container-fluid">
        <% include ../_share/header %>
        <main class="container">
            <div class="jumbotron">
                <h1 class="display-6">Hãng <%= brand.name %></h1>
                <div class="row">
                    <div class="col-2">
                        <img src="<%= brand.logo %>" class="img-fluid">
                    </div>
                </div>
            </div>
            <table class="table table-stripped">
                <thead>
                    <th>Tên xe</th>
                    <th width="70">Ảnh</th>
                    <th>Giá</th>
                </thead>
                <tbody>
                    <% for(let i = 0; i < cars.length; i++){ %>
                        <tr>
                            <td><%= cars[i].name %></td>
                            <td>
                                <img src="<%= cars[i].image %>" class="img-fluid">
                            </td>
                            <td><%= cars[i].price %></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </main>
    </div>
    <% include ../_share/script %>
</body>
</html>

