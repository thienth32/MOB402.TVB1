<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách danh mục</title>
    <% include ../_share/style %>
</head>
<body>
    <div class="container-fluid">
        <% include ../_share/header %>
        <main class="container">
            <h4>Danh sách hãng xe</h4>
            <div class="row">
                <div class="col-4">
                    <form action="" method="get">
                        <div class="form-group">
                            <label for="">Tìm kiếm</label>
                            <input type="text" name="keyword" 
                                value="<%= keyword %>"
                                class="form-control">
                        </div>
                    </form>
                    <div class="d-flex justify-content-center">
                        <button type="submit" class="btn btn-sm btn-primary">Tìm kiếm</button>
                    </div>
                </div>
            </div>
            <table class="table table-stripped">
                <thead>
                    <th>Tên hãng</th>
                    <th width="100">Logo</th>
                    <th>
                        <a href="/brands/create" class="btn btn-sm btn-success">Tạo mới</a>
                    </th>
                </thead>
                <tbody>
                    <% for(let i = 0; i < brands.length; i++){ %>
                        <tr>
                            <td><%= brands[i].name %></td>
                            <td>
                                <img src="<%= brands[i].logo %>" class="img-thumbnail">
                            </td>
                            <td>
                                <a href="/brands/<%= brands[i].id %>"
                                    class="btn btn-sm btn-info">Chi tiết</a>
                                &nbsp;
                                <a href="/brands/update/<%= brands[i].id %>"
                                    class="btn btn-sm btn-primary">Cập nhật</a>
                                &nbsp;
                                <a href="/brands/remove/<%= brands[i].id %>"
                                    onclick="return confirm('Bạn có chắc chắn thực hiện xóa danh mục?')"
                                    class="btn btn-danger btn-sm">Xóa</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </main>
    </div>
    <% include ../_share/script %>
</body>
</html>